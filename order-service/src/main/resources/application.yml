spring:
  application:
    name: order-service

  profiles:
    default: local

server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

logging:
  level:
    root: INFO
    "[ru.ticketcraft]": INFO
